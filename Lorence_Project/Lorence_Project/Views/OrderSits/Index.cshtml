@model IEnumerable<Lorence_Project.Models.OrderSit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SitName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.arrived)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.confirmedByAdmin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SitName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.arrived)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.confirmedByAdmin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderSitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderSitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderSitID })
        </td>
    </tr>
}

</table>
